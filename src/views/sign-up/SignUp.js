import firebase from 'firebase';
import { auth } from '../../services/firebase-config';

import './SignUp.css';

export const SignUp = () => {
  return <form name="signUpForm">
    <h3>Sign up</h3>
  </form>;
}